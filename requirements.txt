flask==2.3.2             # Web framework for backend API
flask-cors==4.0.0        # Enable CORS for frontend-backend communication
gunicorn==20.1.0         # Production server for Flask (required for Render/Heroku)
tensorflow-cpu>=2.16.1  # Use latest stable version
pillow==10.0.0           # Image processing (MRI uploads)
numpy==1.24.3            # Array handling for model input
